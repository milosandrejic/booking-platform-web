import { colors } from './colors.js';
import { typography, fontFamily, fontWeights } from './typography.js';
import { spacing, borderRadius, shadows, breakpoints } from './spacing.js';

// Main theme object - simple and focused
export const theme = {
  colors,
  typography,
  fontFamily,
  fontWeights,
  spacing,
  borderRadius,
  shadows,
  breakpoints
};

export default theme;
