{
  "name": "@booking-platform-shared/utils",
  "version": "0.1.0",
  "private": true,
  "description": "Shared utilities and helpers",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Building utils package'",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "clean": "rm -rf dist node_modules package-lock.json yarn.lock pnpm-lock.yaml"
  },
  "devDependencies": {
    "eslint": "^9"
  }
}